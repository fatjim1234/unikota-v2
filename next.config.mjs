/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Next.js 16: instrumentation.ts is stable and enabled by default
  // (the old experimental.instrumentationHook flag was removed).
};

export default nextConfig;
